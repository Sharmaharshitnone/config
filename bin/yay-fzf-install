#!/bin/bash
# yay-fzf-install - Interactive AUR package installer with fzf

fzf_args=(
  --multi
  --preview 'yay -Sia {1}'
  --preview-label='alt-p: toggle | alt-b/B: PKGBUILD | alt-j/k: scroll | tab: multi-select'
  --preview-label-pos='bottom'
  --preview-window 'down:65%:wrap'
  --bind 'alt-p:toggle-preview'
  --bind 'alt-d:preview-half-page-down,alt-u:preview-half-page-up'
  --bind 'alt-k:preview-up,alt-j:preview-down'
  --bind 'alt-b:change-preview:yay -Gp {1} | bat --language=bash --color=always --style=numbers'
  --bind 'alt-B:change-preview:yay -Sia {1}'
  --color 'pointer:cyan,marker:cyan'
  --prompt='Select AUR packages > '
  --header='Press TAB to select | ALT-b for PKGBUILD | ENTER to install | ESC to cancel'
)

pkg_names=$(yay -Slqa | fzf "${fzf_args[@]}")

if [[ -n "$pkg_names" ]]; then
  echo "$pkg_names" | tr '\n' ' ' | xargs yay -S --needed
  sudo updatedb 2>/dev/null
  echo -e "\n✓ Installation complete. Press ENTER to exit..."
  read
fi